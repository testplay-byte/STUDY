---
subject: mathematics
book_title: null
batch: M-1
chapter_folder: Chapter-01-Functions-and-Graphs
chapter_number: 1
chapter_title: Functions and Graphs
page_image: 2
page_printed: 8
section: "1.1 Function; 1.1.1 Domain of a Function; How to Find the Domain of a Function"
exercise: null
content_type: theory
has_figures: true
figures_count: 2
source_image: ../../../Raw/Mathematics/Unit-01-Functions-and-Graphs/0002.jpg
converted_at: "2026-09-01"
converted_by: "agent-1a (glm-vision)"
notes: ""
---

# Page 2 — Unit 01: Functions and Graphs

> 📄 Original scan: [0002.jpg](../../../Raw/Mathematics/Unit-01-Functions-and-Graphs/0002.jpg) · printed page 8

## 1.1 Function

A function $f$ from a set A to a set B assigns to each element of A exactly one element of B. The set A is called the domain of the function and the set B is called the co-domain of the function.

Mathematically, it is written as $f : \text{A} \to \text{B}$ and is read as $f$ is a function from A to B.

For example, if we are given two sets $\text{A} = \{-1, 1, 2\}$ and $\text{B} = \{1, 4, 9\}$,

then $f = \{(-1, 1), (1, 1), (2, 4)\}$ is a function because each element of the set A is assigned to exactly one element of the set B. i.e. there is no repetition in the first element of ordered pairs in $f$. The first element of each ordered pair in $f$ is called pre-image while its corresponding second element is called image of the first element. For example, in $(2, 4)$, $2$ is the pre-image of $4$ and $4$ is the image of $2$.

[Figure F1]

If $x$ is independent variable and $y$ is dependent variable, then in general, a function $f$ from A to B is written as $f(x) = y$.

For example, in the above example,
$f(-1) = 1, f(1) = 1$ and $f(2) = 4$

**Explanation:** As discussed above, a function relates an input to an output.

For example, if a tree grows 15 cm every year and the height $h$ of the tree is related to its age as follows:

$$h(\text{age}) = \text{age} \times 15$$

then the height of the tree after 10 year is $h(10) = 10 \times 15 = 150$ cm
$\therefore$ '$h(10) = 150$' is like saying 10 is related to 150 or $10 \to 150$
Here, 10 is the input and 150 is the output of the function.

[Figure F2]

## 1.1.1 Domain of a Function

The set of all possible values of independent variable which qualify as inputs to a function is known as the domain of the function. In the above example,
Domain of function $f = \text{Dom } f = \{-1, 1, 2\}$

> **Key Facts**
>
> A function in which real numbers are used is called a real valued function.

**How to Find the Domain of a Function**

To find the domain, we ensure that there is no zero in the denominator of a fraction and no negative sign inside a square root. In general, the set of all real numbers is considered as the domain of a function subject to some restrictions. For example:

(i) When the given function is of the form $f(x) = 3x + 8$ or $f(x) = x^3 + 2x - 5$, the domain will be “the set of all real numbers”.

(ii) When the given function is of the form $f(x) = \frac{1}{x-2}$ the domain will be the set of all real numbers except 2.

## Figures on this page

### Figure F1 — Mapping diagram (top right)
- **Type:** geometric-diagram
- **Caption/Number:** (none printed)
- **Description:** A mapping diagram showing two oval sets labeled 'A' (left) and 'B' (right). Set A contains elements -1, 1, and 2. Set B contains elements 1, 4, and 9. Arrows point from -1 to 1, 1 to 1, and 2 to 4. An overarching curved arrow labeled 'f' points from set A to set B.
- **Mathematical meaning:** Illustrates the definition of a function $f: \text{A} \to \text{B}$ where each element in the domain A maps to exactly one element in the co-domain B.

### Figure F2 — Tree photograph (middle right)
- **Type:** photo
- **Caption/Number:** (none printed)
- **Description:** A color photograph of a large, mature deciduous tree with a thick trunk and dense green foliage, standing on a grassy area under a light sky.
- **Mathematical meaning:** Serves as a real-world contextual example for the function $h(\text{age}) = \text{age} \times 15$, relating the independent variable (age) to the dependent variable (height).
