---
subject: mathematics
book_title: null
batch: M-1
chapter_folder: Chapter-01-Functions-and-Graphs
chapter_number: 1
chapter_title: FUNCTIONS AND GRAPHS
page_image: 4
page_printed: 10
section: "1.2 Types of Functions; 1.2.1 Into Function; 1.2.2 Onto (Surjective) Function"
exercise: null
content_type: theory
has_figures: true
figures_count: 2
source_image: ../../../Raw/Mathematics/Unit-01-Functions-and-Graphs/0004.jpg
converted_at: "2026-09-01"
converted_by: "agent-1a (glm-vision)"
notes: ""
---

# Page 4 — Unit 01: Functions and Graphs

> 📄 Original scan: [0004.jpg](../../../Raw/Mathematics/Unit-01-Functions-and-Graphs/0004.jpg) · printed page 10

**Example 2:**

Find the domain and range of function $g(x) = \sqrt{x - 3}$.

**Solution:** $g(x) = \sqrt{x - 3}$

The given function is defined for all real numbers $x$ greater than or equal to 3.

Thus, the domain of $g(x)$ is $[3, \infty)$.

If we put $x = 3$ in the given function, we get $g(3) = 0$.

For all real values of $x$, greater than 3, we get an output greater than 0.

Hence, the range of $g(x)$ is $[0, \infty)$.

## 1.2 Types of Functions

### 1.2.1 Into Function

A function $f$: A $\rightarrow$ B is said to be into function if there exists at least one element or more than one element in B, which does not have any pre-images in A, which simply means that every element of the codomain is not mapped with elements of the domain. i.e., $\text{range}(f) \neq \text{B}$.

In the adjoining diagram, $f = \{(a, 1), (b, 3), (c, 3)\}$ is an into function.

[Figure F1]

Some examples of into functions are:

*   $f(x) = \sin x$ where $f$: R $\rightarrow$ R is into function because it doesn't cover all values in the interval R.
*   $g(x) = x^2$ where $g$: R $\rightarrow$ R is into function because it doesn't map to any negative real numbers.
*   $h(x) = e^x$ where $h$: R $\rightarrow [0, \infty)$ is into function because it doesn't map to zero.

### 1.2.2 Onto (Surjective) Function

For any two non-empty sets A and B, a function $f : A \rightarrow B$ will be onto if every element of set B is an image of some element of set A. i.e., for every $y \in B$ there exists an element $x$ in A such that $f(x) = y$ which implies $\text{rang}(f) = \text{B}$.

In the adjoining diagram, $f = \{(a, 2), (b, 1), (c, 3)\}$ is an onto function.

[Figure F2]

Some examples of onto functions are:

*   $f(x) = x$ (Identity function)
*   $g(x) = e^x$ when $g$: R $\rightarrow \text{R}^+$ (Exponential function)
*   $h(x) = x^2$ (Square function)
*   $m(x) = x^3$ (Cubic function)
*   $p(x) = c$ (Constant function)

## Figures on this page

### Figure F1 — Into function diagram (right side)
- **Type:** geometric-diagram
- **Caption/Number:** (none printed)
- **Description:** Two ovals labeled 'A' (domain) and 'B' (codomain). An arrow labeled '$f$' points from A to B. Set A contains elements {a, b, c}. Set B contains elements {1, 2, 3, 4}. Arrows show mappings: a maps to 1, b maps to 3, c maps to 3. Element 2 and element 4 in set B have no incoming arrows.
- **Mathematical meaning:** Illustrates an into function where the range $\{1, 3\}$ is a proper subset of the codomain $\{1, 2, 3, 4\}$.

### Figure F2 — Onto function diagram (bottom right)
- **Type:** geometric-diagram
- **Caption/Number:** (none printed)
- **Description:** Two ovals labeled 'A' (domain) and 'B' (codomain). An arrow labeled '$f$' points from A to B. Set A contains elements {a, b, c}. Set B contains elements {1, 2, 3}. Arrows show mappings: a maps to 2, b maps to 1, c maps to 3. Every element in set B has at least one incoming arrow.
- **Mathematical meaning:** Illustrates an onto (surjective) function where the range equals the codomain $\{1, 2, 3\}$.
